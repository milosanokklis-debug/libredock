# This is a general enviromental file used for the creation of the instance.
# sets varibles used for configuration of services that do not have their own dedicated env files

# set the timezone of the services
TZ=America/New_York
# setting the PUID for the process (arbirtary but needs to be different
PUID=1000
PGID=1000

# sets the database user and the database name for the librenms
MYSQL_DATABASE=librenms
MYSQL_USER=librenms
# sets the SQL password. CHANGE THIS ON INSTALL
MYSQL_PASSWORD=asupersecretpassword
